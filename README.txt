TITLE:
        

SOFTWARES REQUIRED:
        1. Node js
        2. MySql
        3. vite+React

DEPENDENCY FOR BACKEND:

        "dependencies": {
        "body-parser": "^2.2.0",
        "cors": "^2.8.5",
        "express": "^5.1.0",
        "mysql": "^2.18.1",
        "mysql2": "^3.14.1",
        "nodemailer": "^7.0.3",
        "nodemon": "^3.1.10"
        }

DEPENDENCY FOR FRONTEND:

        "dependencies": {
    "@tailwindcss/vite": "^4.1.13",
    "axios": "^1.12.2",
    "chart.js": "^4.5.0",
    "react": "^19.1.1",
    "react-chartjs-2": "^5.3.0",
    "react-dom": "^19.1.1",
    "react-hot-toast": "^2.6.0",
    "react-router-dom": "^7.9.3"
  }

step 1:First run the llm.py 
step 2:Then run the backed end with npm start
step 3:Then run the frontend with npm run dev
step 4:Access the dashboard
       Open the frontend in your browser.
       On the dashboard, click Evaluate.
       This will call the LLM API and return the gauge score.
